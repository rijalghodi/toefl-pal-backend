name: Deploy to IDCloudhost

on:
  push:
    branches: [main] # or your deploy branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ vars.VPS_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -p ${{ vars.VPS_PORT }} ${{ vars.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Build Docker image
        run: docker build -t ${{ vars.APP_NAME }} .

      - name: Save image to tarball
        run: docker save ${{ vars.APP_NAME }} > app.tar

      - name: Copy Docker image to VPS
        run: |
          scp -P ${{ vars.VPS_PORT }} app.tar ${{ vars.VPS_USER }}@${{ vars.VPS_HOST }}:/tmp/app.tar

      - name: Load & run container on VPS
        run: |
          ssh -p ${{ vars.VPS_PORT }} ${{ vars.VPS_USER }}@${{ vars.VPS_HOST }} << 'EOF'
            docker load < /tmp/app.tar
            docker rm -f ${{ vars.APP_NAME }} || true
            docker run -d --name ${{ vars.APP_NAME }} -p ${{ vars.APP_PORT }}:10000 ${{ vars.APP_NAME }}
          EOF
